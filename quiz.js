
// Simple Quiz Auto-Grader (expand for full 20 Qs)
function gradeQuiz() {
  let score = 0;
  // Check radios, e.g., if (document.querySelector('input[name="q1"]:checked').value === 'a') score++;
  alert(`Your score: ${score}/5. Review answers below!`);
}